
services:
  valkey:
    image: valkey/valkey:latest
    container_name: valkey
    ports:
      - "6379:6379"
    environment:
      - VALKEY_PASSWORD=12345
    volumes:
      - valkey_data:/data
    restart: unless-stopped

volumes:
  valkey_data: