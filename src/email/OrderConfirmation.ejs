<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - Vibly</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #f6f7f9;
            padding: 24px;
            color: #0f172a;
        }
        
        .container {
            max-width: 560px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 16px;
            padding: 28px;
            box-shadow: 0 8px 24px rgba(15,23,42,0.06);
        }
        
        .brand {
            font-weight: 700;
            font-size: 18px;
            color: #0f172a;
            margin-bottom: 8px;
            text-align: center;
        }
        
        .logo {
            width: 120px;
            height: auto;
            margin: 0 auto 16px;
            display: block;
        }
        
        .title {
            font-weight: 700;
            font-size: 22px;
            margin: 8px 0 6px;
            text-align: center;
        }
        
        .desc {
            color: #475569;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 18px;
        }
        
        .order-info {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 16px;
            margin: 20px 0;
        }
        
        .order-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }
        
        .order-row:last-child {
            margin-bottom: 0;
        }
        
        .order-label {
            font-size: 14px;
            color: #64748b;
            font-weight: 500;
        }
        
        .order-value {
            font-size: 14px;
            color: #0f172a;
            font-weight: 600;
        }
        
        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #0f172a;
            margin: 24px 0 16px;
        }
        
        .product-item {
            display: flex;
            margin-bottom: 16px;
            padding-bottom: 16px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .product-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .product-image {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 8px;
            margin-right: 16px;
            border: 1px solid #e2e8f0;
        }
        
        .product-details {
            flex: 1;
        }
        
        .product-name {
            font-size: 16px;
            font-weight: 600;
            color: #0f172a;
            margin-bottom: 4px;
        }
        
        .product-specs {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 4px;
        }
        
        .product-price {
            font-size: 16px;
            font-weight: 700;
            color: #0f172a;
        }
        
        .summary-section {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 16px;
            margin: 20px 0;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }
        
        .summary-row:last-child {
            margin-bottom: 0;
            border-top: 1px solid #e2e8f0;
            padding-top: 8px;
            font-weight: 700;
        }
        
        .summary-label {
            font-size: 14px;
            color: #64748b;
        }
        
        .summary-value {
            font-size: 14px;
            color: #0f172a;
            font-weight: 600;
        }
        
        .shipping-section {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 16px;
            margin: 20px 0;
        }
        
        .shipping-title {
            font-size: 16px;
            font-weight: 700;
            color: #0f172a;
            margin-bottom: 12px;
        }
        
        .shipping-text {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 8px;
            line-height: 1.6;
        }
        
        .shipping-address {
            font-size: 14px;
            color: #0f172a;
            line-height: 1.6;
            margin-bottom: 12px;
        }
        
        .btn-wrap {
            text-align: center;
            margin: 24px 0;
        }
        
        .btn {
            display: inline-block;
            background: #0b0b0b;
            color: #ffffff;
            text-decoration: none;
            padding: 12px 20px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 14px;
        }
        
        .track-info {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 16px;
            margin: 20px 0;
            text-align: center;
        }
        
        .track-title {
            font-size: 16px;
            font-weight: 700;
            color: #0f172a;
            margin-bottom: 8px;
        }
        
        .track-desc {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 12px;
        }
        
        .order-number {
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace;
            font-size: 14px;
            color: #0f172a;
            background: #ffffff;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            padding: 8px 12px;
            margin: 8px 0;
            display: inline-block;
        }
        
        .footer {
            text-align: center;
            margin-top: 24px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
        }
        
        .footer-text {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 8px;
        }
        
        .footer-brand {
            font-size: 12px;
            color: #94a3b8;
        }
        
        @media (max-width: 600px) {
            body {
                padding: 16px;
            }
            
            .container {
                padding: 20px;
            }
            
            .product-item {
                flex-direction: column;
                text-align: center;
            }
            
            .product-image {
                margin-right: 0;
                margin-bottom: 12px;
            }
            
            .order-row {
                flex-direction: column;
                text-align: left;
            }
            
            .order-value {
                margin-top: 2px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="brand">
            <img src="https://res.cloudinary.com/dnfknlwos/image/upload/v1759596976/logo_p8rjah.png" alt="Vibly Logo" class="logo">
            Vibly
        </div>
        
        <div class="title">🎉 Order Confirmed!</div>
        <p class="desc">Hi <strong><%= user.name %></strong>, thank you for your order! We're excited to get your new clothes ready for you.</p>
        
        <!-- Order Information -->
        <div class="order-info">
            <div class="order-row">
                <span class="order-label">Order Number: </span>
                <span class="order-value"><%= order.orderId %></span>
            </div>
            <div class="order-row">
                <span class="order-label">Order Date: </span>
                <span class="order-value"><%= new Date(order.orderedAt).toLocaleDateString('en-IN') %></span>
            </div>
            <div class="order-row">
                <span class="order-label">Status: </span>
                <span class="order-value">Confirmed</span>
            </div>
        </div>
        
        <!-- Product Items -->
        <div class="section-title">Order Items</div>
        <% order.items.forEach(function(item) { %>
        <div class="product-item">
            <img src="<%= item.product.image.secure_url || item.product.image || '/placeholder-product.jpg' %>" 
                 alt="<%= item.product.name %>" 
                 class="product-image">
            <div class="product-details">
                <div class="product-name"><%= item.product.name %></div>
                <div class="product-specs">
                    Size: <%= item.size %> | Color: <%= item.color.name %> | Qty: <%= item.quantity %>
                </div>
                <div class="product-price">₹<%= (item.amount.price * item.quantity).toLocaleString('en-IN') %></div>
            </div>
        </div>
        <% }); %>
        
        <!-- Order Summary -->
        <div class="summary-section">
            <div class="section-title">Order Summary</div>
            
            <div class="summary-row">
                <span class="summary-label">Subtotal: </span>
                <span class="summary-value">₹<%= order.items.reduce((sum, item) => sum + (item.amount.price * item.quantity), 0).toLocaleString('en-IN') %></span>
            </div>
            
            <div class="summary-row">
                <span class="summary-label">Shipping: </span>
                <span class="summary-value">
                    <% if (order.paymentMethod === 'COD') { %>
                        ₹50
                    <% } else if (order.shippingCharges && order.shippingCharges > 0) { %>
                        ₹<%= order.shippingCharges.toLocaleString('en-IN') %>
                    <% } else { %>
                        Free
                    <% } %>
                </span>
            </div>
            
            <div class="summary-row">
                <span class="summary-label">Total: </span>
                <span class="summary-value">
                    <% 
                        const subtotal = order.items.reduce((sum, item) => sum + (item.amount.price * item.quantity), 0);
                        let shippingAmount = 0;
                        if (order.paymentMethod === 'COD') {
                            shippingAmount = 50;
                        } else {
                            shippingAmount = order.shippingCharges || 0;
                        }
                        const totalAmount = subtotal + shippingAmount;
                    %>
                    ₹<%= totalAmount.toLocaleString('en-IN') %>
                </span>
            </div>
        </div>
        
        <!-- Shipping Information -->
        <div class="shipping-section">
            <div class="shipping-title">Shipping Information</div>
            <div class="shipping-text">
                <strong>Estimated Delivery:</strong> 3-5 business days
            </div>
            <div class="shipping-text">
                <strong>Shipping Address:</strong>
            </div>
            <div class="shipping-address">
                <%= order.shippingInfo.address %><br>
                <%= order.shippingInfo.city %>, <%= order.shippingInfo.state %><br>
                <%= order.shippingInfo.country %> - <%= order.shippingInfo.postalCode %><br>
                Phone: <%= order.shippingInfo.phone %>
            </div>
        
        </div>
        
        <!-- Track Order Section -->
        <div class="track-info">
            <div class="track-title">Track Your Order</div>
            <div class="track-desc">Click the button below to track your order status</div>
            <div class="order-number"><%= order.orderId %></div>
            <div class="btn-wrap">
                <a class="btn" href="https://www.vibly.in/user/track-order?orderId=<%= order.orderId %>">Track Order</a>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="footer-text">Thank you for shopping with Vibly!</div>
            <div class="footer-text">We appreciate your business and look forward to serving you again.</div>
            <div class="footer-brand">
                Vibly E-commerce Store<br>
                © 2025 Vibly. All rights reserved.
            </div>
        </div>
    </div>
</body>
</html>